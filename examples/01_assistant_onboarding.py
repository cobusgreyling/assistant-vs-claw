#!/usr/bin/env python3
"""Onboarding Agent (Assistant) — each hire sees only their own data."""

from identity_models.cli import run_assistant_onboarding

if __name__ == "__main__":
    run_assistant_onboarding()